using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyActSet : MonoBehaviour
{
    // Enemy의 moveing()에 넣을 코드를 Enemy 종류마다 구분해서 이 스크립트에 나열하려 했었던 잔재


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
